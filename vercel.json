{
    "version": 2,
    "buildCommand": "pnpm build",
    "outputDirectory": "apps/dist/playground/browser",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}