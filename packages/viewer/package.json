{
  "name": "@eraser/viewer",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@eraser/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "jest --passWithNoTests --runInBand",
    "watch": "tsc -b --watch"
  }
}
