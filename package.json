{
  "name": "eraser-renderer",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -w --filter \"./packages/*\" run build",
    "test": "pnpm -w --filter \"./packages/*\" run test"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "ts-node": "^10.9.1",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.5",
    "pnpm": "10.0.0"
  }
}
